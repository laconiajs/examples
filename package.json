{
  "name": "@laconia-examples/examples",
  "version": "0.0.1",
  "description": "Examples of laconia",
  "scripts": {
    "clean": "npm run clean:lerna",
    "clean:lerna": "lerna clean --yes || true",
    "postinstall": "lerna bootstrap",
    "test:cloud": "lerna exec -- npm run cloud-test"
  },
  "devDependencies": {
    "@laconia/test": "^0.16.0",
    "aws-sdk": "^2.400.0",
    "jest": "^24.1.0",
    "lerna": "^3.11.0",
    "serverless": "^1.37.1",
    "supertest": "^3.4.2"
  }
}
