service:
  name: laconia-examples-serverless-typescript

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs8.10
  region: ${opt:region, 'eu-west-1'}

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
