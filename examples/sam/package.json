{
  "name": "@laconia-examples/sam",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "../../node_modules/.bin/jest",
    "deploy": "./install.sh",
    "undeploy": "echo \"Error: Unsupported by SAM, go to AWS console and delete the laconia-examples-sam cloudformation stack\" && exit 1"
  },
  "dependencies": {
    "@laconia/core": "^0.16.0"
  }
}
