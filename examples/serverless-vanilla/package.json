{
  "name": "@laconia-examples/serverless-vanilla",
  "version": "0.0.0",
  "scripts": {
    "test": "jest --coverage unit-test/",
    "test:integration": "npm run deploy && AWS_REGION=eu-west-1 jest integration-test/ && npm run undeploy",
    "deploy": "sls deploy",
    "undeploy": "sls remove"
  },
  "dependencies": {
    "@laconia/adapter-api": "^1.4.0",
    "@laconia/core": "^1.4.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.523.0",
    "jest": "^24.9.0",
    "serverless": "^1.51.0",
    "serverless-offline": "^5.10.1"
  }
}
