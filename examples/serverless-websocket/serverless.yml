service: laconia-examples-serverless-websocket

provider:
  name: aws
  runtime: nodejs8.10
  region: ${opt:region, 'eu-west-1'}

functions:
  uppercase:
    handler: handler.handler
    events:
      - websocket:
          route: laconia
