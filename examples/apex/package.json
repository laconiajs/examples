{
  "name": "@laconia-examples/apex",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "../../node_modules/.bin/jest",
    "deploy": "apex deploy",
    "undeploy": "apex delete -f",
    "cloud-test": "npm run deploy && npm run test && npm run undeploy"
  },
  "dependencies": {
    "@laconia/core": "^0.16.0"
  }
}
